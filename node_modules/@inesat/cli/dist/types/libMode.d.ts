import { Configuration } from 'webpack';
export declare type buildLibType = 'var' | 'module' | 'assign' | 'assign-properties' | 'this' | 'window' | 'self' | 'global' | 'commonjs' | 'commonjs2' | 'commonjs-module' | 'amd' | 'amd-require' | 'umd' | 'umd2' | 'jsonp' | 'system' | 'esm';
declare type FileNameType = (format: string) => string;
export declare type LibModeType = {
    name?: string;
    /**
     *  入口文件 可以只设置 entry.name 代替 name，但要遵循 js 变量的命名规则
     */
    entry: Configuration['entry'];
    /**
     * fileName
     * @default [format]/[name].js 建议 format 为目录 避免不同格式代码混淆
     */
    fileName?: FileNameType | string;
    formats: buildLibType[];
    external?: Configuration['externals'];
};
export {};
