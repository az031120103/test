import { cliOptionsType } from "../types";
declare class Init {
    templates: any;
    private checkScriptRun;
    checkData(url: string): Promise<any>;
    setup(cliOptions: cliOptionsType): Promise<void>;
    selectTemplate(): Promise<void>;
    /**
     * 下载仓库
     * @param repoPath
     * @param localPath
     * @param branch
     */
    downloadRepo(repoPath: string, localPath: string, branch: string): Promise<void>;
}
declare const _default: Init;
export default _default;
