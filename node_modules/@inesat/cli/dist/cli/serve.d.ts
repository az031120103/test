import { Express } from 'express';
declare class Serve {
    app: Express;
    resourcePath: string;
    constructor();
    startLogger({ httpsOptions, host, port, publicPath }: any): void;
    setup(): Promise<void>;
}
declare const _default: Serve;
export default _default;
