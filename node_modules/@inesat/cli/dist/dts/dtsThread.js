"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const dts_1 = __importDefault(require("./dts"));
const fast_glob_1 = __importDefault(require("fast-glob"));
const path_1 = __importDefault(require("path"));
const { parentPort } = require('worker_threads');
parentPort.on('message', async (payload) => {
    const options = JSON.parse(payload);
    const { appAbsSrc } = options;
    if (options) {
        const dts = new dts_1.default();
        dts.setup(options);
        const pathStr = path_1.default.join(appAbsSrc, '**/*.(ts|tsx)').replace(/\\/g, '/');
        const dtslist = await (0, fast_glob_1.default)([pathStr]);
        // console.log('dtslist', dtslist, appSrc, `${appSrc}/**/*.(ts|tsx)`)
        dtslist.map(d => {
            dts.emit(d);
        });
        dts.createFile();
        parentPort.postMessage('finish');
    }
});
//# sourceMappingURL=dtsThread.js.map