import { LibModeType } from "../types";
import { Configuration } from 'webpack';
import WPModule from './module';
declare class WPLibMode {
    libConfig: LibModeType;
    wpconfigs: Configuration[];
    isDev: boolean;
    module: WPModule;
    constructor();
    setup(): Promise<Configuration[]>;
    private resetWpchain;
    private resetConfig;
    private initBuildLib;
    private libTarget;
}
declare const _default: WPLibMode;
export default _default;
