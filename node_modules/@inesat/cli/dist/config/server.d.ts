import { Configuration } from 'webpack-dev-server';
export declare type ServerConfig = {
    /**
     * 访问 host
     * @default '0.0.0.0'
     */
    host?: string;
    /**
     * 访问 端口
     * @default 3000
     */
    port?: number;
    /**
     * 自动切换端口
     * @default false
     */
    /**
     * 自动打开
     * @default false
     */
    open?: Configuration['open'];
    /**
     * 热重载
     * @default true
     */
    hot?: Configuration['hot'];
};
export declare type ServerOptions = Configuration & ServerConfig;
export declare type ResolveServerConfig = Configuration & Required<ServerConfig>;
export declare const initServer: (op?: ServerOptions) => ResolveServerConfig;
